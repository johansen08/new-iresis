<?php
defined('BASEPATH') OR exit('No direct script access allowed');

/**
 * TEMPLATE kredensial — file ini ikut di-commit, JANGAN isi nilai asli.
 *
 * Cara pakai di mesin/server baru:
 *   1. Salin file ini menjadi application/config/secrets.php
 *   2. Isi nilainya sesuai environment (lokal / produksi)
 *
 * secrets.php sudah masuk .gitignore dan tidak akan ikut ter-commit.
 */
return array(

	// --- Database utama -------------------------------------------------
	'db_hostname' => '127.0.0.1',
	'db_username' => 'root',
	'db_password' => '',
	'db_database' => 'iresis_prod',

	// Koneksi persisten ke MySQL. Opsional -- kalau baris ini tidak ada,
	// nilainya FALSE, yaitu perilaku lama (koneksi baru tiap request).
	//
	// Nyalakan HANYA kalau server DB berada di mesin lain, dan hanya SETELAH
	// max_connections di server DB dinaikkan melebihi ThreadsPerChild Apache
	// di mesin aplikasi (150). Catatan lengkap + angka pengukurannya ada di
	// application/config/database.php.
	// 'db_pconnect' => TRUE,

	// --- Token auth endpoint Cron (HTTP ?token=) --------------------------
	// Samakan dengan IRESIS_CRON_TOKEN di scripts/secrets_local.py.
	'cron_token' => 'isi-token-cron',

	// --- Pusher ----------------------------------------------------------
	'pusher_app_id'     => 'isi-app-id',
	'pusher_app_key'    => 'isi-app-key',
	'pusher_app_secret' => 'isi-app-secret',
	'pusher_cluster'    => 'ap1',

	// --- Video packing ---------------------------------------------------
	// Folder tempat rekaman video packing disimpan (di luar document root).
	// Opsional -- kalau baris ini tidak ada, dipakai C:/video-packing/.
	// Folder dev di PC yang sama sebaiknya diarahkan ke tempat lain supaya
	// tidak menimpa rekaman produksi.
	// 'video_packing_dir' => 'C:/video-packing/',

	// --- Foto produk (cadangan offline) -----------------------------------
	// Folder salinan foto tblsku.link_foto yang diunduh cron sinkron_foto_sku,
	// dilayani Apache lewat Alias /foto-produk/ (conf/extra/httpd-iresis-foto-produk.conf).
	// Opsional -- kalau baris ini tidak ada, dipakai C:/foto-produk/. Kalau diubah,
	// Alias di Apache harus diarahkan ke folder yang sama.
	// 'foto_produk_dir' => 'C:/foto-produk/',

	// Lokasi ffmpeg untuk finalisasi rekaman (remux WebM agar punya durasi,
	// konversi MP4 atas permintaan CS) -- dijalankan Cron::finalisasi_video.
	// Opsional -- kalau tidak ada, dipakai 'ffmpeg' dari PATH. ffprobe harus
	// ada di folder yang sama. Tanpa ffmpeg, rekaman tetap bisa diputar.
	// 'ffmpeg_path' => 'C:/ffmpeg/bin/ffmpeg.exe',

	// Lokasi php.exe (CLI) yang dipakai Cs::minta_video_mp4 untuk melepas worker
	// konversi MP4 di latar belakang begitu tombol "Siapkan MP4" ditekan.
	// Opsional -- kalau tidak ada, ditebak dari folder induk extension_dir
	// (C:/xampp/php/php.exe). Tanpa ini MP4 tetap dibuat oleh task cron tiap menit.
	// 'php_cli_path' => 'C:/xampp/php/php.exe',

	// --- Khusus folder dev (docs/LINGKUNGAN_DEV.md) -------------------------
	// Keduanya opsional; produksi TIDAK perlu mengisinya.
	//
	// Nama cookie session. Dev di host yang sama wajib beda dari produksi
	// (bawaan 'siresi_session'), kalau tidak session-nya tercampur.
	// 'sess_cookie_name' => 'iresis_dev_session',
	//
	// FALSE = Pusher_lib tidak mengirim notifikasi (bawaan TRUE).
	// 'pusher_aktif' => FALSE,
	//
	// Akun login smoke test tests/smoke_scan.php (docs/UJI_REGRESI.md §2.3):
	// webmaster (role 1) yang aktif di iresis_dev. uji_base_url opsional,
	// bawaannya http://localhost/iresis-dev/.
	// 'uji_username' => 'isi-username',
	// 'uji_password' => 'isi-password',
	// 'uji_base_url' => 'http://localhost/iresis-dev/',
);
