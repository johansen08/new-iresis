<?php
defined('BASEPATH') OR exit('No direct script access allowed');

/**
 * TEMPLATE kredensial — file ini ikut di-commit, JANGAN isi nilai asli.
 *
 * Cara pakai di mesin/server baru:
 *   1. Salin file ini menjadi application/config/secrets.php
 *   2. Isi nilainya sesuai environment (lokal / produksi)
 *
 * secrets.php sudah masuk .gitignore dan tidak akan ikut ter-commit.
 */
return array(

	// --- Database utama -------------------------------------------------
	'db_hostname' => '127.0.0.1',
	'db_username' => 'root',
	'db_password' => '',
	'db_database' => 'iresis-prod',

	// --- Video packing ---------------------------------------------------
	// Folder penyimpanan rekaman Scan Resi Packer (Webcam). DI LUAR webroot.
	// Kalau key ini tidak ada, kode memakai C:/video-packing.
	// Pindah drive cukup mengubah baris ini -- tidak ada path yang tersimpan
	// di database.
	'video_packing_path' => 'C:/video-packing',

	// --- WhatsApp gateway -----------------------------------------------
	// wa_api_token juga dipakai sebagai token auth endpoint Cron.
	'wa_api_url'      => 'http://localhost:3000',
	'wa_api_token'    => 'isi-token-wa-gateway',
	'wa_target_group' => 'xxxxxxxxxxxxxxxxxx@g.us',

	// --- Pusher ----------------------------------------------------------
	'pusher_app_id'     => 'isi-app-id',
	'pusher_app_key'    => 'isi-app-key',
	'pusher_app_secret' => 'isi-app-secret',
	'pusher_cluster'    => 'ap1',
);
